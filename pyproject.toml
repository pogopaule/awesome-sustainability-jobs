[project]
name = "awesome-sustainability-jobs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "polars>=1.28.1",
    "pyyaml>=6.0.2",
    "rich>=14.0.0",
    "streamlit>=1.44.1",
]

[dependency-groups]
dev = [
    "ipython>=9.2.0",
]
